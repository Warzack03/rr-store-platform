import Link from "next/link";
import { Flash } from "@/features/admin/components/flash";
import { EmptyState, PageHeading } from "@/features/admin/components/page-heading";
import { archiveDrop, duplicateDrop } from "@/features/admin/server/drop-actions";
import { getPrismaClient } from "@/server/db/client";

const labels = { DRAFT: "Borrador", PUBLISHED: "Publicado", ARCHIVED: "Archivado" } as const;
export default async function DropsPage({ searchParams }: { searchParams: Promise<{ ok?: string; error?: string }> }) { const [drops, query] = await Promise.all([getPrismaClient().drop.findMany({ orderBy: { updatedAt: "desc" }, include: { _count: { select: { dropProducts: true, orders: true } } } }), searchParams]); return <div className="space-y-6"><PageHeading title="Drops" description="Campañas, calendario, surtido y precios." action={{ href: "/admin/drops/nuevo", label: "Nuevo drop" }} /><Flash searchParams={query} />{drops.length === 0 ? <EmptyState>Aún no hay drops.</EmptyState> : <div className="grid gap-4 lg:grid-cols-2">{drops.map((drop) => <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" key={drop.id}><div className="flex justify-between gap-3"><div><h2 className="text-xl font-bold">{drop.title}</h2><p className="text-sm text-slate-500">{labels[drop.status]}{drop.isPrimary ? " · Principal" : ""}</p></div><span className="text-sm text-slate-500">{drop._count.dropProducts} productos</span></div><p className="mt-3 text-sm">{drop.startsAt ? drop.startsAt.toLocaleString("es-ES") : "Sin inicio"} — {drop.endsAt ? drop.endsAt.toLocaleString("es-ES") : "Sin fin"}</p><div className="mt-4 flex flex-wrap gap-2"><Link className="rounded border border-slate-300 px-3 py-2 text-sm font-semibold" href={`/admin/drops/${drop.id}`}>Editar</Link><Link className="rounded border border-slate-300 px-3 py-2 text-sm font-semibold" href={`/admin/drops/${drop.id}/preview`}>Vista previa</Link><form action={duplicateDrop}><input name="id" type="hidden" value={drop.id} /><button className="rounded border border-slate-300 px-3 py-2 text-sm font-semibold" type="submit">Duplicar</button></form>{drop.status !== "ARCHIVED" ? <form action={archiveDrop}><input name="id" type="hidden" value={drop.id} /><button className="rounded border border-red-300 px-3 py-2 text-sm font-semibold text-red-700" type="submit">Archivar</button></form> : null}</div></article>)}</div>}</div>; }
